import React from 'react'
import Camera from '../Camera'

const Mobile = () => <div>
    <div style={{
        backgroundColor: 'black',
        position: 'fixed',
        width: '100%',
        height: '100%'
    }}>
        <Camera />
    </div>
</div>

export default Mobile
